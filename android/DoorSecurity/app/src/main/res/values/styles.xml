<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="android:Theme.Holo.Light">
        <item name="android:textColor">@color/whiteText</item>
        <item name="android:windowBackground">@color/background</item>
        <item name="android:colorPrimaryDark">@color/dark</item>
        <item name="android:colorPrimary">@color/primary</item>
        <item name="android:colorAccent">@color/accent</item>
        <item name="android:actionBarStyle">@style/actionBar</item>
        <item name="android:buttonStyle">@style/tnt_button</item>
        <item name="android:textColorHint">@color/textHint</item>
        <item name="android:editTextBackground">@drawable/tnt_edit_text</item>
        <item name="android:scrollViewStyle">@style/tnt_scrollView</item>

    </style>

    <style name="actionBar" parent="android:Widget.Holo.ActionBar.Solid">
        <item name="android:background">@color/primary</item>
        <item name="android:titleTextStyle">@style/actionBarTitle</item>
        <item name="android:displayOptions">showTitle</item>
    </style>

    <style name="actionBarTitle" parent="android:TextAppearance.Holo.Widget.ActionBar.Title">
        <item name="android:textColor">@color/whiteText</item>
    </style>

    <style name="tnt_button" parent="android:Widget.Holo.Button">
        <item name="android:background">@drawable/tnt_button_old</item>
        <item name="android:padding">0dp</item>
        <item name="android:minWidth">88dp</item>
        <item name="android:minHeight">36dp</item>
        <item name="android:layout_margin">3dp</item>
        <item name="android:elevation">1dp</item>
        <item name="android:translationZ">1dp</item>
        <item name="android:textColor">@color/whiteText</item>
    </style>
    <style name="tnt_scrollView" parent="android:Widget.Holo.ScrollView">
        <item name="android:overScrollFooter">@color/extraDark</item>
        <item name="android:overScrollHeader">@color/extraDark</item>
    </style>

</resources>
